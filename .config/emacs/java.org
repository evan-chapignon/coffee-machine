#+AUTHOR: Evan DELEPINE
#+DATE: <2025-10-27 Mon>
#+property: header-args :results none

* Java

Un bloc de code pour l'utilisation de java dans emacs, pour être
honnête, je recommande toujours l'utilisation d'intelliJ pour du java poussé

#+begin_src elisp
  (use-package lsp-mode
    :commands lsp
    :init
    (add-hook 'java-mode-hook #'lsp-deferred))


  (use-package lsp-java
    :after lsp-mode
    :config
  )


  (use-package corfu
    :init (global-corfu-mode))

  (use-package flycheck
    :init (global-flycheck-mode))
  
  (use-package dap-mode
    :after lsp-mode
    :config
    
    (require 'dap-java)
    
    (add-hook 'lsp-after-initialize-hook #'dap-auto-configure-mode)
    )


  (use-package projectile
    :init (projectile-mode +1)
    :bind-keymap
    ("C-c p" . projectile-command-map))

  (use-package treemacs
    :bind ("C-x t t" . treemacs))
#+end_src
